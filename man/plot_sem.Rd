% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sem.R
\name{plot_sem}
\alias{plot_sem}
\alias{autoplot.sem_model}
\title{Plot a SEM path diagram}
\usage{
plot_sem(
  model,
  layout = "auto",
  what = "std",
  theme = "classic",
  rotation = NULL,
  residuals = FALSE,
  covariances = TRUE,
  sig_stars = TRUE,
  r_squared = FALSE,
  fit_indices = NULL,
  title = NULL,
  ...
)

\method{autoplot}{sem_model}(object, ...)
}
\arguments{
\item{model}{A fitted model object (e.g. from \code{\link[lavaan:sem]{lavaan::sem()}}) or a
\link{sem_model} object created by \code{\link[=extract_sem]{extract_sem()}}}

\item{layout}{Layout algorithm: \code{"auto"} (default), \code{"tree"}, \code{"tree2"},
\code{"circle"}, \code{"spring"}}

\item{what}{What values to display on paths: \code{"std"} (standardized, default),
\code{"est"} (unstandardized), \code{"label"} (parameter labels)}

\item{theme}{Theme name or a sem_theme object: \code{"classic"} (default),
\code{"modern"}, \code{"apa"}, \code{"minimal"}, \code{"nature"}, \code{"dark"}, \code{"colorful"}}

\item{rotation}{Tree layout direction: \code{"TB"}, \code{"BT"}, \code{"LR"}, \code{"RL"}.
NULL for auto-detection.}

\item{residuals}{Logical, whether to draw residual variance loops
(default FALSE)}

\item{covariances}{Logical, whether to draw covariance paths (default TRUE)}

\item{sig_stars}{Logical, whether to show significance stars (default TRUE)}

\item{r_squared}{Logical, whether to display R-squared inside endogenous
nodes (default FALSE)}

\item{fit_indices}{Character vector of fit index names to annotate, or NULL}

\item{title}{Optional plot title}

\item{...}{Additional arguments passed to layout functions}
}
\value{
A ggplot2 object
}
\description{
The main function of the ggsemplot package. Takes a fitted SEM model or a
\link{sem_model} object and produces a publication-quality ggplot2 path diagram.
}
\examples{
\dontrun{
library(lavaan)
model <- '
  visual  =~ x1 + x2 + x3
  textual =~ x4 + x5 + x6
  speed   =~ x7 + x8 + x9
'
fit <- cfa(model, data = HolzingerSwineford1939)

# One-line beautiful plot
plot_sem(fit)

# With options
plot_sem(fit, theme = "modern", layout = "tree", rotation = "LR")

# Customizable via ggplot2 + syntax
plot_sem(fit) + ggplot2::labs(title = "My CFA Model")
}
}
